{% if flash %}
<div class="row justify-content-center">
    {% for message in flash %}
        <div class="col-md-4 alert alert-{{ message.type }}" role="alert">
            {{ message.message }}
        </div>
    {% endfor %}
</div>
{% endif %}